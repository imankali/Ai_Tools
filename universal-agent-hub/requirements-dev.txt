# Development dependencies (lint, type-check, tests)
# pip install -r requirements.txt -r requirements-dev.txt
pytest>=7.4
pytest-asyncio>=0.23
pytest-cov>=4.1
mypy>=1.8
ruff>=0.4
black>=24.0

# Optional extras used by the tools (recommended for local development)
psutil>=5.9
ddgs>=9.0
# playwright>=1.40   # install the package AND run: playwright install chromium
