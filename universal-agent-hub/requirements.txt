# ---------------------------------------------------------------------------
# Universal Agent Hub — runtime dependencies
# Install: pip install -r requirements.txt
# ---------------------------------------------------------------------------
openai>=1.40,<4.0
pydantic>=2.5,<3.0
pydantic-settings>=2.1,<3.0
python-dotenv>=1.0
rich>=13.0
aiohttp>=3.9
requests>=2.31

# ---------------------------------------------------------------------------
# Dev / test extras
# ---------------------------------------------------------------------------
pytest>=7.4
pytest-asyncio>=0.23
pytest-cov>=4.1
mypy>=1.8
ruff>=0.4
black>=24.0

# ---------------------------------------------------------------------------
# Optional extras (also declared in pyproject.toml)
# ---------------------------------------------------------------------------
# psutil>=5.9          # CPU/RAM/disk metrics used by the `system_info` tools
# ddgs>=9.0            # richer DuckDuckGo results for `web_search`
# playwright>=1.40     # real browser automation for the `browser_*` tools
